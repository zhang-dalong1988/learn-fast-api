# 表单模型

您可以在 FastAPI 中使用 **Pydantic 模型** 来声明**表单字段**。

> **注意**
>
> 自 FastAPI `0.113.0` 版本起支持此功能。🤓

## 用于表单的 Pydantic 模型

您只需声明一个包含您希望作为**表单字段**接收的字段的 **Pydantic 模型**，然后将参数声明为 `Form` 即可。

### Python 3.9+

```python
from typing import Annotated

from fastapi import FastAPI, Form
from pydantic import BaseModel

app = FastAPI()

class FormData(BaseModel):
    username: str
    password: str

@app.post("/login/")
async def login(data: Annotated[FormData, Form()]):
    return data
```

> 🤓 其他版本和变体

#### Python 3.8+ - 非 Annotated

```python
from fastapi import FastAPI, Form
from pydantic import BaseModel
from typing_extensions import Annotated

app = FastAPI()

class FormData(BaseModel):
    username: str
    password: str

@app.post("/login/")
async def login(data: Annotated[FormData, Form()]):
    return data
```

> **提示**
>
> 如果可能，请优先使用 `Annotated` 版本。

#### Python 3.8+ - 不使用 Annotated

```python
from fastapi import FastAPI, Form
from pydantic import BaseModel

app = FastAPI()

class FormData(BaseModel):
    username: str
    password: str

@app.post("/login/")
async def login(data: FormData = Form()):
    return data
```

**FastAPI** 将从请求的**表单数据**中为**每个字段** **提取**数据，并提供您定义的 Pydantic 模型。

## 查看文档

您可以在 `/docs` 的文档 UI 中进行验证。

![表单模型文档界面](https://fastapi.tiangolo.com/img/tutorial/request-form-models/image01.png)

## 禁用额外的表单字段

在一些特殊用例中（可能不常见），您可能希望将表单字段**限制**为仅在 Pydantic 模型中声明的那些字段，并**禁止**任何**额外**字段。

> **注意**
>
> 自 FastAPI `0.114.0` 版本起支持此功能。🤓

你可以使用 Pydantic 的模型配置来 `forbid` 任何 `extra` 字段

### Python 3.9+

```python
from typing import Annotated

from fastapi import FastAPI, Form
from pydantic import BaseModel

app = FastAPI()

class FormData(BaseModel):
    username: str
    password: str
    model_config = {"extra": "forbid"}

@app.post("/login/")
async def login(data: Annotated[FormData, Form()]):
    return data
```

> 🤓 其他版本和变体

#### Python 3.8+ - 非 Annotated

```python
from fastapi import FastAPI, Form
from pydantic import BaseModel
from typing_extensions import Annotated

app = FastAPI()

class FormData(BaseModel):
    username: str
    password: str
    model_config = {"extra": "forbid"}

@app.post("/login/")
async def login(data: Annotated[FormData, Form()]):
    return data
```

> **提示**
>
> 如果可能，请优先使用 `Annotated` 版本。

#### Python 3.8+ - 不使用 Annotated

```python
from fastapi import FastAPI, Form
from pydantic import BaseModel

app = FastAPI()

class FormData(BaseModel):
    username: str
    password: str
    model_config = {"extra": "forbid"}

@app.post("/login/")
async def login(data: FormData = Form()):
    return data
```

如果客户端尝试发送一些额外的数据，他们将收到一个**错误**响应。

例如，如果客户端尝试发送以下表单字段：

- `username`: `Rick`
- `password`: `Portal Gun`
- `extra`: `Mr. Poopybutthole`

他们将收到一个错误响应，告知他们字段 `extra` 是不允许的。

```json
{
    "detail": [
        {
            "type": "extra_forbidden",
            "loc": ["body", "extra"],
            "msg": "Extra inputs are not permitted",
            "input": "Mr. Poopybutthole"
        }
    ]
}
```

## 总结

您可以在 FastAPI 中使用 Pydantic 模型来声明表单字段。😎