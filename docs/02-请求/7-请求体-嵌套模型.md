# 请求体 - 嵌套模型 - FastAPI 教程

## 概要

本章介绍 FastAPI 中嵌套模型的使用，这是构建复杂 API 的关键技术。你将学习如何处理列表、嵌套对象和复杂数据结构，以便在现实应用中定义精确的数据模型。

### 核心内容

- 列表和集合类型
- 模型嵌套与深度嵌套
- 特殊验证类型（如 HttpUrl）

---

使用 FastAPI，你可以定义、验证、文档化和使用任意深度嵌套的模型（感谢 Pydantic）。

## 1. 列表字段

你可以定义一个属性为子类型。例如，一个 Python `list`：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()  # 创建 FastAPI 应用

class Item(BaseModel):  # 定义商品模型
    name: str  # 商品名称（必需）
    description: str | None = None  # 商品描述（可选）
    price: float  # 商品价格（必需）
    tax: float | None = None  # 税率（可选）
    tags: list = []  # 标签列表，默认为空列表

@app.put("/items/{item_id}")  # PUT 路由用于更新商品
async def update_item(item_id: int, item: Item):  # item 参数将是 Item 模型实例
    results = {"item_id": item_id, "item": item}
    return results
```

这将使 `tags` 成为一个列表，虽然没有声明列表元素的类型。

## 2. 带类型参数的列表字段

但 Python 有一种特定的方式来声明带有内部类型或"类型参数"的列表：

### 2.1 导入 typing 的 List

在 Python 3.9 及以上版本中，你可以使用标准的 `list` 来声明这些类型注解。💡

但在 3.9 之前的 Python 版本（3.6 及以上）中，你首先需要从标准 Python 的 `typing` 模块导入 `List`：

```python
# Python 3.8+
from typing import List, Union  # List 用于列表类型注解，Union 用于可选类型

from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    description: Union[str, None] = None
    price: float
    tax: Union[float, None] = None
    tags: List[str] = []  # 明确指定为字符串列表

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

### 2.2 其他版本和变体

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    description: str | None = None  # Python 3.10+ 使用 | 语法
    price: float
    tax: float | None = None
    tags: list[str] = []  # 使用标准 list 语法

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

```python
# Python 3.9+
from typing import Union  # Python 3.9 仍需要 Union

from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    description: Union[str, None] = None
    price: float
    tax: Union[float, None] = None
    tags: list[str] = []  # Python 3.9+ 可以使用 list[str]

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

### 2.3 声明带有类型参数的 list

要声明具有类型参数（内部类型）的类型，如 `list`、`dict`、`tuple`：

- 如果你使用的 Python 版本低于 3.9，从 `typing` 模块导入它们的等效版本
- 使用方括号 `[` 和 `]` 将内部类型作为"类型参数"传递

在 Python 3.9 中，它会是：

```python
my_list: list[str]  # 标准 Python 3.9+ 语法
```

在 3.9 之前的 Python 版本中，它会是：

```python
from typing import List

my_list: List[str]  # 旧版本语法
```

这些都是标准的 Python 类型声明语法。

对具有内部类型的模型属性使用相同的标准语法。

因此，在我们的示例中，我们可以使 `tags` 特别成为"字符串列表"：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: list[str] = []  # 明确指定为字符串列表

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

## 3. 集合类型

但然后我们思考一下，意识到标签不应该重复，它们可能是唯一的字符串。

而 Python 有一个用于唯一项集合的特殊数据类型，即 `set`。

然后我们可以将 `tags` 声明为字符串集合：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: set[str] = set()  # 使用 set 自动去重

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

### 3.1 其他版本和变体

```python
# Python 3.8+
from typing import Set, Union  # Set 用于集合类型

from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Item(BaseModel):
    name: str
    description: Union[str, None] = None
    price: float
    tax: Union[float, None] = None
    tags: Set[str] = set()  # 使用 Set 类型

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

这样，即使你收到包含重复数据的请求，它也会被转换为唯一项的集合。

并且每当你输出该数据时，即使源数据有重复，它也会作为唯一项的集合输出。

它也会被相应地注解/记录在文档中。

## 4. 嵌套模型

Pydantic 模型的每个属性都有一个类型。

但该类型本身可以是另一个 Pydantic 模型。

因此，你可以声明具有特定属性名称、类型和验证的深度嵌套的 JSON "对象"。

所有这些，都可以任意嵌套。

### 4.1 定义子模型

例如，我们可以定义一个 `Image` 模型：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Image(BaseModel):  # 定义图像模型
    url: str  # 图像 URL
    name: str  # 图像名称

class Item(BaseModel):  # 商品模型包含图像
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: set[str] = set()
    image: Image | None = None  # 可选的图像嵌套模型

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

### 4.2 使用子模型作为类型

然后我们可以将其用作属性的类型：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Image(BaseModel):  # 图像子模型
    url: str
    name: str

class Item(BaseModel):  # 商品主模型
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: set[str] = set()
    image: Image | None = None  # 嵌套 Image 模型作为类型

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

这意味着 FastAPI 会期望类似于以下的请求体：

```json
{
  "name": "Foo",
  "description": "The pretender",
  "price": 42.0,
  "tax": 3.2,
  "tags": ["rock", "metal", "bar"],
  "image": {
    "url": "http://example.com/baz.jpg",
    "name": "The Foo live"
  }
}
```

同样，仅通过该声明，使用 FastAPI 你可以获得：

- 编辑器支持（自动补全等），即使对于嵌套模型
- 数据转换
- 数据验证
- 自动文档生成

## 5. 特殊类型和验证

除了像 `str`、`int`、`float` 等普通的单一类型外，你可以使用继承自 `str` 的更复杂的单一类型。

要查看你拥有的所有选项，请查看 Pydantic 的类型概述。你将在下一章看到一些示例。

例如，正如在 `Image` 模型中我们有一个 `url` 字段，我们可以将其声明为 Pydantic 的 `HttpUrl` 实例，而不是 `str`：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl  # 导入 HttpUrl 特殊类型

app = FastAPI()

class Image(BaseModel):
    url: HttpUrl  # 使用 HttpUrl 类型进行 URL 验证
    name: str

class Item(BaseModel):
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: set[str] = set()
    image: Image | None = None

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

字符串将被检查为有效的 URL，并相应地在 JSON Schema / OpenAPI 中记录。

## 6. 包含子模型列表的属性

你也可以使用 Pydantic 模型作为 `list`、`set` 等的子类型：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl

app = FastAPI()

class Image(BaseModel):
    url: HttpUrl
    name: str

class Item(BaseModel):
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: set[str] = set()
    images: list[Image] | None = None  # 包含 Image 模型的列表

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

### 6.1 其他版本和变体

```python
# Python 3.8+
from typing import List, Set, Union

from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl

app = FastAPI()

class Image(BaseModel):
    url: HttpUrl
    name: str

class Item(BaseModel):
    name: str
    description: Union[str, None] = None
    price: float
    tax: Union[float, None] = None
    tags: Set[str] = set()
    images: Union[List[Image], None] = None  # 使用 Union 和 List

@app.put("/items/{item_id}")
async def update_item(item_id: int, item: Item):
    results = {"item_id": item_id, "item": item}
    return results
```

这将期望（转换、验证、文档化等）类似于以下的 JSON 请求体：

```json
{
  "name": "Foo",
  "description": "The pretender",
  "price": 42.0,
  "tax": 3.2,
  "tags": ["rock", "metal", "bar"],
  "images": [
    {
      "url": "http://example.com/baz.jpg",
      "name": "The Foo live"
    },
    {
      "url": "http://example.com/dave.jpg",
      "name": "The Baz"
    }
  ]
}
```

**注意**：注意 `images` 键现在如何包含一个图像对象列表。

## 7. 深度嵌套模型

你可以定义任意深度嵌套的模型：

```python
# Python 3.10+
from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl

app = FastAPI()

class Image(BaseModel):  # 最深层：图像模型
    url: HttpUrl
    name: str

class Item(BaseModel):  # 中间层：商品模型
    name: str
    description: str | None = None
    price: float
    tax: float | None = None
    tags: set[str] = set()
    images: list[Image] | None = None  # 包含图像列表

class Offer(BaseModel):  # 顶层：报价模型
    name: str
    description: str | None = None
    price: float
    items: list[Item]  # 包含商品列表

@app.post("/offers/")  # 创建报价
async def create_offer(offer: Offer):
    return offer
```

### 7.1 其他版本和变体

```python
# Python 3.8+
from typing import List, Set, Union

from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl

app = FastAPI()

class Image(BaseModel):
    url: HttpUrl
    name: str

class Item(BaseModel):
    name: str
    description: Union[str, None] = None
    price: float
    tax: Union[float, None] = None
    tags: Set[str] = set()
    images: Union[List[Image], None] = None

class Offer(BaseModel):
    name: str
    description: Union[str, None] = None
    price: float
    items: List[Item]  # 商品列表

@app.post("/offers/")
async def create_offer(offer: Offer):
    return offer
```

**注意**：注意 `Offer` 如何包含一个 `Item` 列表，而 `Item` 又包含一个可选的 `Image` 列表。

## 8. 纯列表体

如果你期望的 JSON 请求体的顶级值是一个 JSON `array`（Python `list`），你可以在函数参数中声明类型，与在 Pydantic 模型中相同：

```python
# Python 3.9+
from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl

app = FastAPI()

class Image(BaseModel):
    url: HttpUrl
    name: str

@app.post("/images/multiple/")  # 接收多个图像的端点
async def create_multiple_images(images: list[Image]):  # 直接接收图像列表
    return images
```

### 8.1 其他版本和变体

```python
# Python 3.8+
from typing import List

from fastapi import FastAPI
from pydantic import BaseModel, HttpUrl

app = FastAPI()

class Image(BaseModel):
    url: HttpUrl
    name: str

@app.post("/images/multiple/")
async def create_multiple_images(images: List[Image]):  # 使用 List 类型
    return images
```

## 9. 无处不在的编辑器支持

你可以随处获得编辑器支持。

即使是列表内的项目：

![编辑器支持示例](https://fastapi.tiangolo.com/img/tutorial/body-nested-models/image01.png)

如果你直接使用 `dict` 而不是 Pydantic 模型，你无法获得这种编辑器支持。

但你也不必担心它们，传入的字典会被自动转换，你的输出也会自动转换为 JSON。

## 10. 任意字典体

你也可以将请求体声明为具有某种类型的键和某种类型的值的 `dict`。

这样，你不必事先知道有效的字段/属性名称是什么（就像 Pydantic 模型的情况）。

如果你想接收你尚不知道的键，这将很有用。

---

另一个有用的案例是当你想要另一种类型的键（例如，`int`）时。

这就是我们在这里要看到的。

在这种情况下，只要它具有带有 `float` 值的 `int` 键，你就会接受任何 `dict`：

```python
# Python 3.9+
from fastapi import FastAPI

app = FastAPI()

@app.post("/index-weights/")  # 接收索引权重
async def create_index_weights(weights: dict[int, float]):  # 键为整数，值为浮点数
    return weights
```

### 10.1 其他版本和变体

```python
# Python 3.8+
from typing import Dict  # 使用 Dict 类型

from fastapi import FastAPI

app = FastAPI()

@app.post("/index-weights/")
async def create_index_weights(weights: Dict[int, float]):  # 字典类型注解
    return weights
```

**提示**：请记住，JSON 只支持 `str` 作为键。

但 Pydantic 有自动数据转换功能。

这意味着，即使你的 API 客户端只能发送字符串作为键，只要这些字符串包含纯整数，Pydantic 就会转换和验证它们。

而你作为 `weights` 接收到的 `dict` 实际上将具有 `int` 键和 `float` 值。

## 11. 总结

使用 FastAPI，你可以获得 Pydantic 模型提供的最大灵活性，同时保持代码的简单、简短和优雅。

但拥有所有好处：

- 编辑器支持（无处不在的自动补全！）
- 数据转换（也称为解析/序列化）
- 数据验证
- 模式文档
- 自动文档生成
